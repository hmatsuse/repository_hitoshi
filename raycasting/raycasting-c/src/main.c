#include <stdio.h>

void initializeWindow() {
    
}

int     main() {
    initializeWindow();
    
    while (1) {
        // processInput();
        // update();
        // render();
    }va_list
    return 0;
};
