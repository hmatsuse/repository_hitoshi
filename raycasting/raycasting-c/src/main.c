#include <stdio.h>

void initializeWindow() {
    
}

int     main() {
    initializeWindow();
    
    while (1) {
        // processInput();
        // update();
        // render();
    }
    return 0;
};
